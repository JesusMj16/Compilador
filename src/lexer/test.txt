fn main() {
    let x = 42;
    let mut y = 0xFF;
    let z = 0b1010;
    let w = 3.14e-2;
    let name = "Hello, World!";
    let mut flag: bool = true;
    let ch: char = 'A';
    let point = coords;
    let value = coords.x;
    
    if x > 10 {
        y += 1;
    }

    match flag {
        true => value = value + 1;
        false => value = value - 1;
    }
    
    // Este es un comentario
    /* Comentario de bloque */
    
    return x + y;
}