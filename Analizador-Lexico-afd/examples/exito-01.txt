fn main() {
    let cinco: i32 = 5; 
    let pi: f64 = 3;
    // Variable mutable
    let mut contador: i32 = 0;
    contador = contador + 1;

    let resultado = cinco * (2 + contador); 

    
    let es_mayor = resultado > 10; 
    let es_igual = (resultado % 2) == 0; 
}